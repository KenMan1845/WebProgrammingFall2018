var options = {
    valueNames: [ 'name']
  };
  
  var values = [ {
    name: "Kenny Manning",
    
  }];
  
  var userList = new List('users', options, values);
  
  userList.add({
    name: "Moshe Plotkin"
    
  });


